{
  "name": "personal-project-root",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix gateway",
    
    "client": "npm run dev --prefix frontend",
    "api": "node gateway/server.js",
    "stock": "cd services/DailyStockService && uvicorn main:app --reload --port 8000",
    
    "dev": "concurrently -n \"FE,API,PY\" -c \"bgBlue.bold,bgMagenta.bold,bgYellow.bold\" \"npm run client\" \"npm run api\" \"npm run stock\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
